import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.airsoftApp123',
  appName: 'Airsoft App',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
